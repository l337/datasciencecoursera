rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    #data <- read.csv("outcome-of-care-measures.csv", colClasses="character")        ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}#rankhospital("TX", "heart failure", "best")
rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    #data <- read.csv("outcome-of-care-measures.csv", colClasses="character")        ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}#rankhospital("TX", "heart failure", "best")
rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    #data <- read.csv("outcome-of-care-measures.csv", colClasses="character")        ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}#rankhospital("TX", "heart failure", "best")
source('~/Documents/coursera/datasciencecoursera/rprogramming/assignment3/rankhospital.R')
rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    #data <- read.csv("outcome-of-care-measures.csv", colClasses="character")        ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}#rankhospital("TX", "heart failure")
rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    #data <- read.csv("outcome-of-care-measures.csv", colClasses="character")        ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}rankhospital("TX", "heart failure", "best")
rankhospital("TX", "heart failure", "best")
rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    data <- read.csv("outcome-of-care-measures.csv", colClasses="character")    str(data)    ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}rankhospital("TX", "heart failure", "best")
rankhospital <- function(state2, outcome2, num = "best") {    ## Read outcome data    data2 <- read.csv("outcome-of-care-measures.csv", colClasses="character")    str(data2)    ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}rankhospital("TX", "heart failure", "best")
rankhospital <- function(state, outcome, num = "best") {    ## Read outcome data    data <- read.csv("outcome-of-care-measures.csv", colClasses="character")    str(data)    ## Check that state and outcome are valid    ## Return hospital name in that state with the given rank    #subregion <- subset(data, data$State==state)    #heartvalues <- suppressWarnings(as.numeric(na.omit(subregion$Hospital.30.Day.Death..Mortality..Rates.from.Heart.Failure)))    #newerlist <- list(name=subregion$Hospital.Name, values=heartvalues)    #head(newerlist)    ## 30-day death rate}rankhospital("TX", "heart failure", "best")
reddit <- read.csv('reddit.csv')
table(reddit$employment.status)
str(reddit)
levels(reddit$age.range)
Library(ggplot2)
qplot(data = reddit, x=age.range)
